# Build output
/build/
/builddir/
/src/build/
*.o
*.a
*.so

# Editor / IDE
.cache/
compile_commands.json
.vscode/
*.swp

# Vendored Android server binary (fetched separately, see README)
/data/scrcpy-server*

# Python venv used to run meson (project-local convenience)
/.meson-venv/

# Distribution build output
/dist/
*.AppImage
*.deb
*.rpm
*.src.rpm
*.pkg.tar
*.pkg.tar.*

# Non-Arch packaging generated locally only
/debian/
/specula.spec
/packaging/appimage/
